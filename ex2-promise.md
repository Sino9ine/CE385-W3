ex2-promise

เอาฟังก์ชันจากข้อ 1 มาทำใหม่ด้วย new Promise แทน callback ใช้ resolve/reject แทน ข้อดีคือ สามารถต่อคำสั่งเป็น chain ด้วย .then() ได้ อ่านง่ายกว่าเดิม มี .catch() ดักทุกข้อผิดพลาด รวมจุดเดียว ปิดท้ายด้วย .finally() ทำงานเสมอไม่ว่าจะสำเร็จหรือไม่ ส่วนโบนัสคือเขียน promisify แปลง callback เป็น Promise อัตโนมัติ ไม่ต้องเขียนซ้ำทุกครั้ง