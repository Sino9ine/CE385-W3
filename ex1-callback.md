ex1-callback

เขียนฟังก์ชัน fetchStudentById(id, callback) ไม่ได้ส่งค่ากลับโดยตรง แต่ใช้ callback แทนกฎคือ ถ้ามี error ให้ส่งมันเป็นอาร์กิวเมนต์ตัวแรก (error‑first) ถ้าไม่มี error ให้ส่งข้อมูลจริงเป็นอาร์กิวเมนต์ตัวที่สอง เราใช้ setTimeout 300 ms เพื่อจำลองฐานข้อมูลช้าปัญหาคือเมื่อมีหลายชั้น callback จะสับสนและอ่านยาก